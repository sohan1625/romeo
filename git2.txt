cd Desktop/raj

git config --global user.name "sohanst1625"
git config --global user.email "sohanst1625@gmail.com"

git init
git remote add origin https://github.com/sohan1625/git2.git

echo "hello">sin.txt
git add .
git commit -m "new commit"
git push origin master

git checkout -b branch1
echo "hellobro">sin1.txt
git add .
git commit -m "first commit"

git switch master
git merge branch1
ls

echo "ram">sin.txt
git add .
git commit -m "first1 commit"

git switch branch1
echo "rammy">sin.txt
git add .
git commit -m "first12 commit"

git switch master
git merge branch1

git add .
git commit -m "first123 commit"

git push origin master
git diff branch1
